# CSS / Compositing Refinement

- Playback presentation defaults to performance (no CSS brightness filter).
- Session-active dashboard uses minimal ambient / reducedEffects.
- Visual Performance Reduced calls adaptive controller FPS/scale hooks.
- No global hardware-acceleration disable.
