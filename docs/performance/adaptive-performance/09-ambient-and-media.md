# Ambient and Express Media

Ambient maps effectiveTier → cinematic/balanced/minimal; session-active forces minimal.

Media network policy is separate from GPU visual tier: residence times, buffering-based tightening, no changes during fragile seek/reconnect, slow recovery.
