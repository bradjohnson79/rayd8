# EffectivePerformanceProfile v1

Includes mode, effectiveTier, targetFps, renderScale, maxDevicePixelRatio, decorative flags, `visualEffectsTier`, separate `mediaNetworkPolicy`, pause flags, reason, lastTierChangeAt, ordinaryTierChangesThisSession, activityState.
