# Amrita Integration

Isolated bundle uses versioned adapter (`applyAmritaAdaptiveProfile` / postMessage). Soak probe may implement the same contract.

Applies FPS/scale/DPR only — never mutates `sessionStartedAt` / pause accumulation used for protocol timing.
