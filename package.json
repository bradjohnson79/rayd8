{
  "name": "rayd8-amrita",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:web": "npm --prefix web run dev",
    "dev:hamsa": "cd hamsa && npx expo start --web --port 8081 --host localhost",
    "dev:api": "npm --prefix api run dev",
    "build:web": "npm --prefix web run build",
    "build:hamsa": "cd hamsa && npx expo export --platform web",
    "build:api": "npm --prefix api run build",
    "lint:web": "npm --prefix web run lint",
    "lint:hamsa": "npm --prefix hamsa run lint",
    "db:generate": "npm --prefix api run db:generate",
    "fixture:mux-soak-auth": "npm --prefix api run fixture:mux-soak-auth",
    "test:mux-stability:smoke": "npm --prefix web run test:mux-stability:smoke",
    "test:mux-stability:soak": "npm --prefix web run test:mux-stability:soak",
    "test:mux-unit": "npm --prefix api run test:mux-ttl && npm --prefix web run test:recovery-machine && npm --prefix web run test:av-sync"
  }
}
